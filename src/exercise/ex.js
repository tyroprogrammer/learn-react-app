import React from 'react';

function ClickableButton(props) {
    /**
     * ✏️ 
     * define content for the button. Maybe call it 'Click Me'?
     * const buttonContent = ....
     */

    /**
     * ✏️ 
     * define a click handler here. For brevity you can just alert with some content
     * const handleClick = ....
     */

    return (
        /**
         * ✏️ 
         * instead of returning null we want to return a button element
         * we have defined the button content above so use that variable
         * and also pass a handler for onClick event to the button element
         */
        null
    );
}

/**
 * 🚨 🚨 DO NOT DELETE OR CHANGE THIS.🚨 🚨
 * This is how you would use your above component and 
 * the output of this code is displayed on the browser
 */
const Usage = (props) => {
    return <ClickableButton />
}

export default Usage;
